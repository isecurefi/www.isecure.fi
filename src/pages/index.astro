---
import { defaultLanguage } from '../utils/languages';
export const prerender = true;

// Simple redirect to the default language version
return Astro.redirect(`/${defaultLanguage}/index.html`);
---
