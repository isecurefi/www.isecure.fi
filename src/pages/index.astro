---
import { defaultLanguage } from '../utils/languages';
export const prerender = true;

// Redirect to the default language version
return Astro.redirect(`/${defaultLanguage === 'fi' ? '' : defaultLanguage}`);
---
