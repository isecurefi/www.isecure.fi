---
import PankkiyhteysPage from "../../components/PankkiyhteysPage.astro";
import { languages } from "../../utils/languages";

export async function getStaticPaths() {
  return languages
    .filter((lang) => lang !== "fi")
    .map((lang) => ({
      params: { lang },
      props: { lang },
    }));
}

const { lang } = Astro.props;
---

<PankkiyhteysPage lang={lang} />
