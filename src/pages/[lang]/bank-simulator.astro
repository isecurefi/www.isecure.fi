---
import BankSimulatorPage from "../../components/BankSimulatorPage.astro";
import { languages } from "../../utils/languages";

export function getStaticPaths() {
  return languages
    .filter((lang) => lang !== "fi")
    .map((lang) => ({ params: { lang }, props: { lang } }));
}

const { lang } = Astro.props;
---

<BankSimulatorPage lang={lang} />
